# empty_suite fixture

Intentionally holds no `.gd` file. Fixture for `tests/runner/test_runner.py`'s
"an existing suite directory with zero `.gd` files still exits 0" regression
test (ticket #40) — this placeholder only exists so git tracks the otherwise
empty directory.
